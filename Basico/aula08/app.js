// Exibe um alerta no navegador.
window.alert('Olá!');

// Exibe um prompt no navegador.
const nome = prompt('Informe o seu nome: ');
const idade = Number(prompt('Informe a sua idade: '));
const maiorIdade = confirm('Em seu país, você é maior de idade?');

console.log(nome, idade, maiorIdade);