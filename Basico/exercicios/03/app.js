const numero = Number(prompt('Informe um número: '));
const outroNumero = Number(prompt('Informe outro número: '));

const adicao = numero + outroNumero;

alert(`A soma entre os valores informados, é: ${adicao}`);