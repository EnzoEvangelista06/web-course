// Tipos de dados: string, number, boolean, undefined, null
const nome = 'Luiz'; // String
const idade = 2; // Number - Int
const peso = 10.5; // Number - Float
const eCrianca = true; // Boolean

const nomeAluno = undefined; // undefined: aponta para local nenhum (não use isto)
const sobrenomeAluno = null; // null: aponta para local nenhum (use isto)

// `typeof`: Verificando o tipo das variáveis
console.log(typeof nome, nome);
console.log(typeof sobrenomeAluno, sobrenomeAluno);