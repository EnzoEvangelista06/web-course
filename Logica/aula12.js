const frutas = ['Pera', 'Maça', 'Uva'];

for (let i in frutas) {
    console.log(i, frutas[i]);
}

const aluno = {
    nome: 'João',
    idade: 16,
}

for (let i in aluno) {
    console.log(i, aluno[i]);
}